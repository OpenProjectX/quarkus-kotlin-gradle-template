version = 0.1.0-SNAPSHOT

kotlin.code.style=official

org.gradle.caching=true
# The Quarkus Gradle plugin is not fully configuration-cache compatible yet,
# so keep it disabled to avoid build failures during augmentation.
org.gradle.configuration-cache=false
