// Core library module: framework-agnostic Kotlin code shared by the extension.
// Put your pure domain/business logic here; the runtime module re-exports it via `api`.
plugins {
    id("buildsrc.convention.kotlin-jvm")
}

dependencies {

}
